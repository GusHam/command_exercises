IMPORTANT: Do ALL exercises inside ~/cli_sandbox to avoid changing important files. Start with:
1) mkdir -p ~/cli_sandbox
2) cd ~/cli_sandbox

1: Create a file called "correspondence.txt" containing these words (one per line): "rain needle rain ditch rain". Then count the lines.

2: If already inside "cli_sandbox", create a directory "recommend" and move into the directory.

3: Create a file called "banish.txt".

4: Delete the last file you created.

5: Create a file called "hill.txt" and immediately delete it.

6: Create a folder named "cli_sandbox".

7: Output what's in the directory.

8: Delete the last file you created.

9: Output hidden files.

10: Create a file "bay.txt" containing: "recession beat recession advice recession". Then output the sorted lines.

11: Create a file called "interference.txt" containing these words (one per line): "needle correspondence needle rain needle". Then count the lines.

12: Open a file in the terminal and see the content.

13: Output the full path you are on.

14: Open a file in the terminal and see the content.

15: Move two folders up (but never leave "cli_sandbox").

16: Move one level up (stay inside "cli_sandbox").

17: Output what's in the directory.

18: Create a file "stock.txt" containing: "evaluate recommend evaluate advantage evaluate". Then output the unique lines.

19: Move one level up (stay inside "cli_sandbox").

20: Create a directory "stock" and then delete it.

21: Delete the last folder you created.

22: Output the sizes of the files in the directory.

23: If there are any directories in the current folder, then move inside any one of them.

24: If already inside "cli_sandbox", create a directory "depression" and move into the directory.

25: Change directory to "cli_sandbox".

26: Output the content of any file in the directory.

27: Create a file called "recommend.txt" and immediately delete it.

28: Delete the last folder you created.

29: Move one level up (stay inside "cli_sandbox").

30: Create a file called "honor.txt" and immediately delete it.

31: Open a file in the terminal and see the content.

32: Create a file "excuse.txt" containing: "terminal shot terminal sister terminal". Then output the unique lines.

33: Create a file "speculate.txt" containing: "rain cater rain stock rain". Then output the sorted lines.

34: Open a file in the terminal and see the content.

35: Create a file "hill.txt" containing: "hill recommend architecture identity difficulty". Then search recursively for "hill".

36: Output the sizes of the files in the directory.

37: Output the sizes of the files in the directory.

38: Create a folder named "cli_sandbox".

39: Use: echo "cousin correspondence appreciate recommend pray" > "responsibility.txt" to create or overwrite "responsibility.txt".

40: If there are any directories in the current folder, then move inside any one of them.

41: Create a file "discover.txt" containing: "beat discover rain medal evaluate". Then search recursively for "discover".

42: Output who the active user is.

43: Output who the active user is.

44: Create a file called "surprise.txt" and immediately delete it.

45: Create a file called "hill.txt" and immediately delete it.

46: Create a file "appreciate.txt" containing: "ethics cater ethics rider ethics". Then output the sorted lines.

47: Output the content of any file in the directory.

48: Create a file called "rain.txt" and immediately delete it.

49: Change directory to "cli_sandbox".

50: Output the full path you are on.

51: Create a file "ditch.txt" containing: "hill discover hill experienced hill". Then output the unique lines.

52: Output what's in the directory.

53: Create a file called "ditch.txt" and immediately delete it.

54: Output the sizes of the files in the directory.

55: Create a file "interference.txt" containing: "ethics terminal ethics architecture ethics". Then output the sorted lines.

56: Output the sizes of the files in the directory.

57: Output the full path you are on.

58: Create two directories "advice" and "pioneer" and move into "pioneer".

59: If there are any directories in the current folder, then move inside any one of them.

60: Create a file "panel.txt" containing: "recession salvation recession stock recession". Then output the unique lines.

61: Delete the last folder you created.

62: Move one level up (stay inside "cli_sandbox").

63: Create a file called "architecture.txt" and immediately delete it.

64: Output hidden files.

65: Create a file "plane.txt" containing: "responsibility cater pioneer plane support". Then search recursively for "plane".

66: Move two folders up (but never leave "cli_sandbox").

67: Delete the last file you created.

68: Delete the last file you created.

69: Move two folders up (but never leave "cli_sandbox").

70: If already inside "cli_sandbox", create a directory "medal" and move into the directory.

71: Create a file "terminal.txt" containing: "terminal rain proportion rain salvation". Then search recursively for "terminal".

72: Output the full path you are on.

73: Create a file "responsibility.txt" containing: "shot proportion shot cater shot". Then output the unique lines.

74: Create a file called "discover.txt" containing these words (one per line): "panel speculate panel surprise panel". Then count the lines.

75: Create a file called "speculate.txt" and immediately delete it.

76: Create two directories "proportion" and "medal" and move into "medal".

77: Create a file "rain.txt" containing: "cater banish sister rain salvation". Then search recursively for "rain".

78: Output the content of any file in the directory.

79: If there are any directories in the current folder, then move inside any one of them.

80: Output who the active user is.

81: If already inside "cli_sandbox", create a directory "identity" and move into the directory.

82: Delete the last file you created.

83: Use: echo "banish speculate evaluate difficulty nerve" > "honor.txt" to create or overwrite "honor.txt".

84: Create a directory "identity" and then delete it.

85: Open a file in the terminal and see the content.

86: Output the full path you are on.

87: Change directory to "cli_sandbox".

88: Change directory to "cli_sandbox".

89: Move one level up (stay inside "cli_sandbox").

90: Create a file "recession.txt" containing: "experienced evaluate experienced relative experienced". Then output the unique lines.

91: Delete the last file you created.

92: Move two folders up (but never leave "cli_sandbox").

93: Output what's in the directory.

94: Delete the last folder you created.

95: Create a file called "regret.txt".

96: Use: echo "degree cater medal cater difficulty" > "stock.txt" to create or overwrite "stock.txt".

97: Move two folders up (but never leave "cli_sandbox").

98: Create a file "support.txt" containing: "evaluate evaluate evaluate relative evaluate". Then output the unique lines.

99: Move one level up (stay inside "cli_sandbox").

100: Output hidden files.

101: Output the full path you are on.

102: Output the content of any file in the directory.

103: Delete the last folder you created.

104: Create a file "proportion.txt" containing: "salvation architecture salvation panel salvation". Then output the sorted lines.

105: Create a directory "hill" and then delete it.

106: Create a file "terminal.txt" containing: "banish degree banish shot banish". Then output the sorted lines.

107: Change directory to "cli_sandbox".

108: Output what's in the directory.

109: Create a file "evaluate.txt" containing: "rider evaluate pen evaluate discover". Then search recursively for "evaluate".

110: Write something in a file called "shallow.txt".

111: If there are any directories in the current folder, then move inside any one of them.

112: Move two folders up (but never leave "cli_sandbox").

113: Output what's in the directory.

114: Open a file in the terminal and see the content.

115: Change directory to "cli_sandbox".

116: Move one level up (stay inside "cli_sandbox").

117: Output the content of any file in the directory.

118: Create a folder named "cli_sandbox".

119: Write something in a file called "identity.txt".

120: Output hidden files.

121: If there are any directories in the current folder, then move inside any one of them.

122: Create a file "rain.txt" containing: "proportion rider bay correspondence rain". Then search recursively for "rain".

123: Create a file "pen.txt" containing: "ditch needle ditch architecture ditch". Then output the unique lines.

124: Delete the last file you created.

125: Delete the last folder you created.

126: If there are any directories in the current folder, then move inside any one of them.

127: Create a file called "medal.txt" containing these words (one per line): "depression rider depression stock depression". Then count the lines.

128: Create a file "appreciate.txt" containing: "rain stock rain panel rain". Then output the unique lines.

129: Move two folders up (but never leave "cli_sandbox").

130: Open a file in the terminal and see the content.

131: Create a file "appreciate.txt" containing: "correspondence interference correspondence bay correspondence". Then output the unique lines.

132: Create a directory "evaluate" and then delete it.

133: Output the full path you are on.

134: Delete the last folder you created.

135: Output the full path you are on.

136: Move one level up (stay inside "cli_sandbox").

137: Output hidden files.

138: Create a file "evaluate.txt" containing: "on evaluate on appreciate on". Then output the sorted lines.

139: Move two folders up (but never leave "cli_sandbox").

140: Create a file called "recession.txt" containing these words (one per line): "pray surprise pray bay pray". Then count the lines.

141: Use: echo "rain nerve depression recession banish" > "support.txt" to create or overwrite "support.txt".

142: If already inside "cli_sandbox", create a directory "ethics" and move into the directory.

143: Create a file called "ethics.txt" containing these words (one per line): "support medal support on support". Then count the lines.

144: Write something in a file called "architecture.txt".

145: Output hidden files.

146: Create a folder named "cli_sandbox".

147: Move one level up (stay inside "cli_sandbox").

148: Change directory to "cli_sandbox".

149: Output what's in the directory.

150: Create a file called "advantage.txt" and immediately delete it.

151: Output the content of any file in the directory.

152: Output hidden files.

153: If already inside "cli_sandbox", create a directory "beat" and move into the directory.

154: Create a file "regret.txt" containing: "regret surprise regret interference regret". Then output the sorted lines.

155: Move one level up (stay inside "cli_sandbox").

156: If there are any directories in the current folder, then move inside any one of them.

157: Move two folders up (but never leave "cli_sandbox").

158: Create a file called "pen.txt" containing these words (one per line): "speculate medal speculate excuse speculate". Then count the lines.

159: Move one level up (stay inside "cli_sandbox").

160: Create a directory "medal" and then delete it.

161: Output hidden files.

162: Output hidden files.

163: Output hidden files.

164: Move two folders up (but never leave "cli_sandbox").

165: If there are any directories in the current folder, then move inside any one of them.

166: Write something in a file called "hill.txt".

167: Create a file called "excuse.txt" containing these words (one per line): "banish appreciate banish rider banish". Then count the lines.

168: Open a file in the terminal and see the content.

169: Create a file called "cousin.txt".

170: Create a file called "banish.txt" and immediately delete it.

171: Output the content of any file in the directory.

172: Output the full path you are on.

173: Open a file in the terminal and see the content.

174: Write something in a file called "responsibility.txt".

175: Write something in a file called "ditch.txt".

176: Create a file called "discover.txt".

177: Create a folder named "cli_sandbox".

178: Delete the last file you created.

179: Create a file "medal.txt" containing: "proportion surprise ethics beat medal". Then search recursively for "medal".

180: Output hidden files.

181: Use: echo "speculate ethics bay depression appreciate" > "pen.txt" to create or overwrite "pen.txt".

182: Write something in a file called "correspondence.txt".

183: Output who the active user is.

184: Output the full path you are on.

185: Create a file "bay.txt" containing: "proportion panel proportion plane proportion". Then output the sorted lines.

186: Write something in a file called "recession.txt".

187: Create two directories "correspondence" and "appreciate" and move into "appreciate".

188: Move one level up (stay inside "cli_sandbox").

189: Change directory to "cli_sandbox".

190: Delete the last folder you created.

191: Create a file "discover.txt" containing: "pioneer excuse pioneer stock pioneer". Then output the sorted lines.

192: Output the content of any file in the directory.

193: Output hidden files.

194: Change directory to "cli_sandbox".

195: Create a directory "sister" and then delete it.

196: Create a file called "difficulty.txt" containing these words (one per line): "bay ethics bay beat bay". Then count the lines.

197: Write something in a file called "excuse.txt".

198: Create a file called "correspondence.txt" containing these words (one per line): "shallow banish shallow bay shallow". Then count the lines.

199: If there are any directories in the current folder, then move inside any one of them.

200: Create a file called "cater.txt" and immediately delete it.

201: Output the sizes of the files in the directory.

202: If there are any directories in the current folder, then move inside any one of them.

203: Create a file called "experienced.txt" and immediately delete it.

204: Create a folder named "cli_sandbox".

205: If already inside "cli_sandbox", create a directory "nerve" and move into the directory.

206: Output the content of any file in the directory.

207: Output who the active user is.

208: Output the sizes of the files in the directory.

209: Move two folders up (but never leave "cli_sandbox").

210: Output what's in the directory.

211: If already inside "cli_sandbox", create a directory "recession" and move into the directory.

212: Create a file called "bay.txt" containing these words (one per line): "hill appreciate hill plane hill". Then count the lines.

213: Output what's in the directory.

214: Move one level up (stay inside "cli_sandbox").

215: Change directory to "cli_sandbox".

216: Output the full path you are on.

217: Output the full path you are on.

218: Use: echo "advice proportion depression cater difficulty" > "recommend.txt" to create or overwrite "recommend.txt".

219: Output what's in the directory.

220: Create a file called "advice.txt" containing these words (one per line): "needle correspondence needle degree needle". Then count the lines.

221: If already inside "cli_sandbox", create a directory "ditch" and move into the directory.

222: If there are any directories in the current folder, then move inside any one of them.

223: Move two folders up (but never leave "cli_sandbox").

224: Open a file in the terminal and see the content.

225: Delete the last file you created.

226: Change directory to "cli_sandbox".

227: Move two folders up (but never leave "cli_sandbox").

228: Open a file in the terminal and see the content.

229: Create a file called "correspondence.txt" containing these words (one per line): "responsibility shallow responsibility rain responsibility". Then count the lines.

230: Change directory to "cli_sandbox".

231: Create a file "banish.txt" containing: "banish interference ethics speculate needle". Then search recursively for "banish".

232: Delete the last file you created.

233: Output what's in the directory.

234: Create a folder named "cli_sandbox".

235: Open a file in the terminal and see the content.

236: Output who the active user is.

237: Change directory to "cli_sandbox".

238: Move two folders up (but never leave "cli_sandbox").

239: Delete the last folder you created.

240: Output what's in the directory.

241: Delete the last file you created.

242: Create a folder named "cli_sandbox".

243: Move one level up (stay inside "cli_sandbox").

244: Output what's in the directory.

245: Open a file in the terminal and see the content.

246: Move two folders up (but never leave "cli_sandbox").

247: Output hidden files.

248: Create a file "architecture.txt" containing: "correspondence panel correspondence cater correspondence". Then output the unique lines.

249: Output the content of any file in the directory.

250: Output hidden files.

251: Use: echo "degree excuse nerve salvation terminal" > "evaluate.txt" to create or overwrite "evaluate.txt".

252: Create a file called "responsibility.txt" and immediately delete it.

253: Move two folders up (but never leave "cli_sandbox").

254: Use: echo "excuse evaluate identity correspondence pioneer" > "architecture.txt" to create or overwrite "architecture.txt".

255: Move one level up (stay inside "cli_sandbox").

256: Create a file called "excuse.txt" and immediately delete it.

257: Change directory to "cli_sandbox".

258: If already inside "cli_sandbox", create a directory "appreciate" and move into the directory.

259: If there are any directories in the current folder, then move inside any one of them.

260: Output the content of any file in the directory.

261: Create a directory "responsibility" and then delete it.

262: Create a file "bay.txt" containing: "pray appreciate pray discover pray". Then output the sorted lines.

263: Create a file called "depression.txt" and immediately delete it.

264: Create a file called "needle.txt" containing these words (one per line): "responsibility pen responsibility difficulty responsibility". Then count the lines.

265: Create a file "regret.txt" containing: "experienced responsibility experienced recommend experienced". Then output the unique lines.

266: Output the content of any file in the directory.

267: Delete the last folder you created.

268: Use: echo "depression evaluate degree regret rider" > "depression.txt" to create or overwrite "depression.txt".

269: Create a file "needle.txt" containing: "needle banish needle experienced needle". Then output the unique lines.

270: Change directory to "cli_sandbox".

271: If already inside "cli_sandbox", create a directory "panel" and move into the directory.

272: Open a file in the terminal and see the content.

273: Output hidden files.

274: Create a file "shot.txt" containing: "bay experienced bay depression bay". Then output the unique lines.

275: Create a folder named "cli_sandbox".

276: Create a directory "surprise" and then delete it.

277: Create a file called "rider.txt" containing these words (one per line): "cousin pen cousin recession cousin". Then count the lines.

278: Create a file "on.txt" containing: "needle advice needle regret needle". Then output the unique lines.

279: Create a file called "experienced.txt" containing these words (one per line): "relative pray relative nerve relative". Then count the lines.

280: Output the content of any file in the directory.

281: Move one level up (stay inside "cli_sandbox").

282: If there are any directories in the current folder, then move inside any one of them.

283: Use: echo "rider panel appreciate correspondence rider" > "pioneer.txt" to create or overwrite "pioneer.txt".

284: Change directory to "cli_sandbox".

285: Create a file "needle.txt" containing: "needle evaluate needle appreciate needle". Then output the unique lines.

286: Create a file "hill.txt" containing: "advice hill stock sister cousin". Then search recursively for "hill".

287: Output the content of any file in the directory.

288: Create a file "banish.txt" containing: "advantage recession advantage hill advantage". Then output the unique lines.

289: Output what's in the directory.

290: Create a file "hill.txt" containing: "ethics difficulty ethics shot ethics". Then output the sorted lines.

291: Delete the last file you created.

292: Output the content of any file in the directory.

293: If there are any directories in the current folder, then move inside any one of them.

294: Output hidden files.

295: Open a file in the terminal and see the content.

296: Output what's in the directory.

297: Output hidden files.

298: Create a file called "correspondence.txt".

299: Create a file called "responsibility.txt" containing these words (one per line): "salvation surprise salvation advantage salvation". Then count the lines.

300: Create a folder named "cli_sandbox".

301: Use: echo "bay nerve discover cousin stock" > "excuse.txt" to create or overwrite "excuse.txt".

302: Output hidden files.

303: Output who the active user is.

304: Create a folder named "cli_sandbox".

305: Change directory to "cli_sandbox".

306: Output the sizes of the files in the directory.

307: Open a file in the terminal and see the content.

308: Create a file called "identity.txt" containing these words (one per line): "recession recommend recession on recession". Then count the lines.

309: If there are any directories in the current folder, then move inside any one of them.

310: Output the sizes of the files in the directory.

311: Create a file called "interference.txt".

312: Move one level up (stay inside "cli_sandbox").

313: Create a file "medal.txt" containing: "salvation recommend salvation medal salvation". Then output the unique lines.

314: Create a file "responsibility.txt" containing: "interference ethics interference plane interference". Then output the unique lines.

315: Create a directory "evaluate" and then delete it.

316: Create a file "interference.txt" containing: "experienced shallow experienced identity experienced". Then output the sorted lines.

317: Output who the active user is.

318: Create two directories "evaluate" and "nerve" and move into "nerve".

319: Delete the last folder you created.

320: Output who the active user is.

321: Use: echo "shot hill ditch proportion discover" > "nerve.txt" to create or overwrite "nerve.txt".

322: Output the content of any file in the directory.

323: Create a file "advice.txt" containing: "excuse medal excuse ethics excuse". Then output the sorted lines.

324: Use: echo "needle architecture support relative pray" > "pray.txt" to create or overwrite "pray.txt".

325: If there are any directories in the current folder, then move inside any one of them.

326: Output what's in the directory.

327: Create two directories "terminal" and "salvation" and move into "salvation".

328: Create a file called "difficulty.txt".

329: Create a file "on.txt" containing: "appreciate recession appreciate relative appreciate". Then output the unique lines.

330: Output who the active user is.

331: Create a file called "excuse.txt" containing these words (one per line): "pioneer ditch pioneer recommend pioneer". Then count the lines.

332: Create a file "advice.txt" containing: "advice salvation rain support relative". Then search recursively for "advice".

333: Output the sizes of the files in the directory.

334: Create a folder named "cli_sandbox".

335: If already inside "cli_sandbox", create a directory "cater" and move into the directory.

336: Move one level up (stay inside "cli_sandbox").

337: Change directory to "cli_sandbox".

338: Create two directories "interference" and "salvation" and move into "salvation".

339: If there are any directories in the current folder, then move inside any one of them.

340: Create a folder named "cli_sandbox".

341: Output hidden files.

342: Move two folders up (but never leave "cli_sandbox").

343: Create a file "excuse.txt" containing: "interference panel terminal excuse panel". Then search recursively for "excuse".

344: Write something in a file called "difficulty.txt".

345: Change directory to "cli_sandbox".

346: Use: echo "relative rain shallow banish panel" > "bay.txt" to create or overwrite "bay.txt".

347: Create a file called "pray.txt" containing these words (one per line): "recommend cater recommend plane recommend". Then count the lines.

348: Output what's in the directory.

349: Change directory to "cli_sandbox".

350: Create a file called "nerve.txt".

